// Return the longest prefix of strings in an array
arr = ["cars", "carpet", "carrier"];

function longest_prefix(arr) {
  const seen = {
    longest: arr[0]
  };

  let str = "";
  
  for (let i = 0; i < arr.length; i++) {
    
    if (arr[0] == arr[1])

  }

  
}

longest_prefix(arr)
