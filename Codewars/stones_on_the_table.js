function solve (stones) {
  let counter = 0;
  const eachStones = stones.split('');

  for (let i = 0; i < eachStones.length; i++) {
    if ()
  }

    console.log(eachStones)
}

// solve('') //0
solve("RRGGBB") //3 );
// solve("RGBRGB"), //0 );
// solve("BGRBBGGBRRR"), //4 );
// solve("GBBBGGRRGRB"), //4
// solve("GBRGGRBBBBRRGGGB") //7 